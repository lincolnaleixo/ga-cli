{
  "name": "ga-cli",
  "version": "1.0.0",
  "description": "Google Analytics reporting CLI and API client.",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "bin": { "ga-cli": "./bin/ga-cli" },
  "scripts": {
    "check": "bun run typecheck && bun run test",
    "test": "bun test src"
  },
  "devDependencies": { "@types/bun": "1.4.0", "typescript": "7.0.2" }
}

